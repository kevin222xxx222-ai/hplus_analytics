import "dotenv/config";
import { createDriveClient } from "../src/lib/import-automation/google-drive/client";
import { executeHeavenCastDriveFile, validateHeavenCastExecuteInput } from "../src/lib/import-automation/google-drive/heaven-cast-execute";
function arg(name: string) { const prefix = `--${name}=`; return process.argv.slice(2).find((value) => value.startsWith(prefix))?.slice(prefix.length); }
async function main() { const driveFileId = arg("drive-file-id"); const confirmProduction = process.argv.includes("--confirm-production"); if (!driveFileId) throw new Error("Usage: npm run drive:execute-heaven-cast -- --drive-file-id=<id> [--confirm-production]"); validateHeavenCastExecuteInput({ driveFileId }); const result = await executeHeavenCastDriveFile({ driveFileId, confirmProduction, client: createDriveClient() }); console.log(`Heaven CAST Drive Execute: ${result.outcome === "SKIPPED" ? "SKIPPED" : "OK"}`); if (result.batchId) console.log(`ImportBatch: ${result.batchId}`); if (result.batchStatus) console.log(`Batch Status: ${result.batchStatus}`); console.log(`Drive State: ${result.outcome === "EXECUTED" ? "REVIEW_REQUIRED" : result.reason || "UNCHANGED"}`); if (result.reviewUrl) console.log(`Review URL: ${result.reviewUrl}`); console.log("Confirm: NOT EXECUTED"); }
main().catch((error: unknown) => { console.error(`Heaven CAST Drive Execute: FAILED — ${error instanceof Error ? error.message : "unknown error"}`); process.exitCode = 1; });
