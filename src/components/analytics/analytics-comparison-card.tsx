import { AvailabilityBadge } from "./availability-badge";
import { TrendIndicator } from "./trend-indicator";
import type { ComparisonViewModel, TrendDirection } from "@/lib/analytics/ui";

export function AnalyticsComparisonCard({ comparison, direction = "UNAVAILABLE", positiveIsBetter = true }: { comparison: ComparisonViewModel; direction?: TrendDirection; positiveIsBetter?: boolean }) {
  return <article className="panel min-w-0 p-4" aria-label={`${comparison.label}との比較`}><div className="flex flex-wrap items-center justify-between gap-2"><h3 className="font-semibold text-slate-800">{comparison.label}</h3>{comparison.availability && <AvailabilityBadge value={comparison.availability} />}</div><div className="mt-3 grid gap-3 sm:grid-cols-3"><div><p className="text-xs text-slate-500">現在値</p><p className="font-semibold">{String(comparison.current ?? "—")}</p></div><div><p className="text-xs text-slate-500">比較値</p><p className="font-semibold">{String(comparison.baseline ?? "—")}</p></div><div><p className="text-xs text-slate-500">差分</p><p className="font-semibold">{String(comparison.difference ?? "—")}</p></div></div><div className="mt-3"><TrendIndicator direction={direction} rate={comparison.rate} baselineLabel={comparison.label} comparisonPeriod={comparison.period} positiveIsBetter={positiveIsBetter} confidence={comparison.confidence} /></div>{comparison.sample && <p className="mt-2 text-xs text-slate-500">Sample: {comparison.sample}</p>}{comparison.availability && comparison.availability !== "VALUE" && <p className="mt-2 text-xs text-slate-500">{comparison.availability === "UNAVAILABLE" ? "基準未接続" : "比較データを確認してください"}</p>}</article>;
}
